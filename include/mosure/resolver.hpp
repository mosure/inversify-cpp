#pragma once

#include <mosure/symbol.hpp>


namespace mosure::inversify {

    // template <typename Interface>
    // struct Resolver {
    //     virtual Interface resolve(Context& context) = 0;
    // };

    // template <typename Interface>
    // struct ConstantResolver : Resolver<Interface> {
    //     Interface resolve(Context& context) override;
    // };

    // template <typename Interface>
    // struct DynamicResolver : Resolver<Interface> {
    //     Interface resolve(Context& context) override;
    // };

    // template <typename Interface>
    // struct InstanceResolver : Resolver<Interface> {
    //     Interface resolve(Context& context) override;
    // };

    // template <typename Interface>
    // struct PointerResolver : Resolver<Interface> {
    //     Interface resolve(Context& context) override;
    // };

}
